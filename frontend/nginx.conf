server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;
  access_log /var/log/nginx/frontend.access.logs;
  access_log /var/log/nginx/frontend.logs;
  error_log /var/log/nginx/frontend.errors.log warn;

  location / {
    try_files $uri /index.html;
  }
}
